function multiplicacion(condicion){
        if (condicion) {
                return "Se envio un verdadero";
        } else {
                return "Se envio un falso";
        }
}

var Bool = false;

alert("Resultado de la funcion multiplicar: "+ multiplicacion(Bool));
